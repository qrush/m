name: StandardRB

jobs:
  standard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
      - name: Standard
        run: bundle exec rake standard
